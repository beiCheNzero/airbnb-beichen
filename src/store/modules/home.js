/*
 * 采用reduxjs/toolkit， RTK方式配置
*/
import { createSlice } from "@reduxjs/toolkit";

const homSlice = createSlice({
  name: 'home',
  initialState: {
    
  },
  reducers: {

  }
})

export default homSlice.reducer